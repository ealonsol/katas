export function chooseBestSum(t: number, k: number, ls: number[]): number {
    const termina: boolean = true;
    /* while (termina) {
        for (let i: number = 0; i < ls[i]; i++ ) {
            console.log('ff');
        }
    } */
    return 0;
}
